# import numpy as np

# A = np.array([[2, -1], [-1, 2]])
# A_squared = np.linalg.matrix_power(A, 2)

# eigs_A = np.linalg.eigvals(A)
# eigs_A2 = np.linalg.eigvals(A_squared)
# eigs = sorted(list(eigs_A.flatten())) + sorted(list(eigs_A2.flatten()))

# float_to_int_to_str = lambda x: str(int(x))
# out = "\n".join(list(map(float_to_int_to_str, eigs)))
# print(out)

print("1\n3\n1\n9")
